apiVersion: config.openshift.io/v1
kind:
metadata:
  name: cluster
spec:
  identityProviders:
- name: htpasswd
  mappingMethod: claim
  type: HTPasswd
  htpasswd:
    fileData:
      name: htpass-secret
